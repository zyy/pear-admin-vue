interface PearConfig {
  themeColor: string;
  routeMode: 'history' | 'hash';
}

const pearConfig: PearConfig = {
  themeColor: '#36b368',
  routeMode: 'history'
}

export { pearConfig as default }
export {
  pearConfig
}
